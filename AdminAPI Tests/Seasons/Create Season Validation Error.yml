info:
  name: Create Season Validation Error
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/seasons"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept-Language
      value: es
  body:
    type: json
    data: "{}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 400", () => {
          expect(res.status).to.equal(400);
        });
        test("content-type is application/problem+json", () => {
          expect(res.headers["content-type"]).to.contain("application/problem+json");
        });
        test("is ValidationProblemDetails with errors object", () => {
          expect(res.body.errors).to.be.an("object");
        });
        test("title is localized in Spanish", () => {
          expect(res.body.title).to.contain("validación");
        });
        test("has validation errors for required fields", () => {
          expect(res.body.errors.Name).to.be.an("array");
          expect(res.body.errors.Code).to.be.an("array");
          expect(res.body.errors.StartDate).to.be.an("array");
          expect(res.body.errors.EndDate).to.be.an("array");
        });
        test("error messages are localized", () => {
          expect(res.body.errors.Name[0]).to.contain("Nombre");
          expect(res.body.errors.Code[0]).to.contain("Código");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
