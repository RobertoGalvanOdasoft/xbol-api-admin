info:
  name: Get Season Not Found
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/seasons/999999999"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 404", () => {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
