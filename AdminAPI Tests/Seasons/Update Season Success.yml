info:
  name: Update Season Success
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/seasons/{{createdSeasonId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Updated Season OC",
        "code": "TST-OC",
        "startDate": "2026-04-01T00:00:00Z",
        "endDate": "2026-09-30T00:00:00Z"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 204", () => {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
