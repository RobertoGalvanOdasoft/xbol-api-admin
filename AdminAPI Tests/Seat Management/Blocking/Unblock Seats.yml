info:
  name: Unblock Seats
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/seat-management/{{scheduleExternalKey}}/unblock"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "seatKeys": ["{{blockSeat1}}", "{{blockSeat2}}", "{{blockSeat3}}"],
        "reason": "Released per sponsor request"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 204", () => {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
