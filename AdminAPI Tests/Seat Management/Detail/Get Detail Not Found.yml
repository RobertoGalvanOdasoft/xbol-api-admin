info:
  name: Get Detail Not Found
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/seat-management/non-existent-key"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 404", () => {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
