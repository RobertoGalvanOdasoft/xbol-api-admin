info:
  name: Hold Seats for Token Tests
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/seat-management/{{scheduleExternalKey}}/hold"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "seats": ["{{holdTokenSeat1}}", "{{holdTokenSeat2}}"]
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", () => {
          expect(res.status).to.equal(200);
        });
        test("response has token", () => {
          expect(res.body.token).to.be.a("string");
        });
        bru.setVar("holdToken", res.body.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
