info:
  name: Delete Suite Success
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/suites/{{createdSuiteId}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 204", () => {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
